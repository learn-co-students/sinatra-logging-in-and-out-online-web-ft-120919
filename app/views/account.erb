<ul>
    <% if Helpers.is_logged_in(session)? %>
    <li><%= "Welcome #{Helpers.current_user(session).username}" %></li>
    <li><%= Helpers.current_user(session).balance %></li>
    
    <% end %>
    </ul>
<a href="/logout">Log Out</a>